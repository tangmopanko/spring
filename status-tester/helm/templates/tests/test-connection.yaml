apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "status-tester.fullname" . }}-test-connection"
  labels:
    {{- include "status-tester.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "status-tester.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
